sensors: []
# Octomap DISABLED for FPS: no depth camera on Dofbot (was /head_mount_kinect/...)
# which doesn't exist -> updater allocated octomap monitor + spammed empty diffs
# into monitored_planning_scene at 1Hz, forcing RViz re-render.
# To re-enable with a real camera, restore:
# sensors:
#   - default_sensor
# default_sensor:
#   far_clipping_plane_distance: 5.0
#   filtered_cloud_topic: filtered_cloud
#   image_topic: /head_mount_kinect/depth_registered/image_raw
#   max_update_rate: 1.0
#   near_clipping_plane_distance: 0.3
#   padding_offset: 0.03
#   padding_scale: 4.0
#   queue_size: 5
#   sensor_plugin: occupancy_map_monitor/DepthImageOctomapUpdater
#   shadow_threshold: 0.2
